<% provide(:title, 'All Users') %>
<h1>All Users</h1>
<%= paginate @users %>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user %>
      <%= link_to full_name(user), user %>
    </li>
  <% end %>
</ul>
<%= paginate @users %>